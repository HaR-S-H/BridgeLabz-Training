using System;
class PerimeterOfRectangle
{
    static void Main()
    {
        double length = 5.0;
        double width = 3.0;
        double perimeter = 2 * (length + width);
        Console.Write(perimeter);
    }
}