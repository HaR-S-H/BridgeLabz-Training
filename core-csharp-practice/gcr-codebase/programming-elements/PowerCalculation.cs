using System;
class PowerCalculation
{
    static void Main()
    {
        double basen = 2.0;
        double ex = 3.0;
        double result = Math.Pow(basen, ex);
        Console.Write(result);
    }
}